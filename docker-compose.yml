services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - path: .env
        required: false
    environment:
      - PORT=5000
    volumes:
      - db-data:/app/server/data

volumes:
  db-data:
